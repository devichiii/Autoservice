{
  "name": "autoservice-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run start:dev -w @autoservice/api",
    "dev:web": "npm run dev -w @autoservice/web",
    "build:api": "npm run build -w @autoservice/api",
    "build:web": "npm run build -w @autoservice/web"
  }
}
